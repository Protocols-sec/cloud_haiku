name: Safe PR POC

on:
  pull_request_target:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v3

      # Simulate access to a secret without revealing it
      - name: Show environment variable keys
        run: |
          echo "Available environment variables:"
          env | grep GITHUB
